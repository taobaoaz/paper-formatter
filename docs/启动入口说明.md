# 📂 启动入口说明

**版本：** v2.0.0  
**更新日期：** 2024-03-18

---

## 🎯 推荐启动方式

### Windows 用户

**双击运行：**
```
启动.bat
```

### Linux/Mac 用户

**终端运行：**
```bash
chmod +x 启动.sh
./启动.sh
```

### 所有平台

**命令行运行：**
```bash
python3 run.py
```

---

## 📋 启动文件清单

| 文件 | 说明 | 平台 | 推荐 |
|------|------|------|------|
| `run.py` | 主启动入口 | 所有 | ✅ 推荐 |
| `launcher.py` | 启动器脚本 | 所有 | ✅ |
| `启动.bat` | Windows 快捷方式 | Windows | ✅ 推荐 |
| `启动.sh` | Linux/Mac 快捷方式 | Linux/Mac | ✅ 推荐 |

---

## 🔄 版本说明

### v2.0 启动入口

**主程序：** `main_refactored.py`

**启动文件：**
- `run.py` → 导入 `main_refactored`
- `launcher.py` → 导入 `main_refactored`

### v1.0 启动入口（旧版）

**主程序：** `main.py`

**说明：** 保留用于兼容，不包含新功能

---

## 🚀 使用方式

### 方式 1：双击启动（最简单）

**Windows:**
```
1. 双击 启动.bat
2. 程序自动运行
```

**Linux/Mac:**
```bash
1. chmod +x 启动.sh
2. ./启动.sh
```

### 方式 2：命令行启动

```bash
cd /home/admin/openclaw/workspace/paper-formatter
python3 run.py
```

### 方式 3：直接运行主程序

```bash
python3 main_refactored.py
```

---

## ⚙️ 依赖安装

首次运行前：

```bash
pip install -r requirements.txt
```

**主要依赖：**
- PyQt5 >= 5.15.0
- python-docx >= 0.8.11
- pywinstyles (可选，Windows 美化)

---

## 📊 启动流程

```
启动脚本 (run.py/启动.bat)
    ↓
导入 main_refactored
    ↓
创建 QApplication
    ↓
创建 MainWindow
    ↓
显示界面
    ↓
运行事件循环
```

---

## 🎯 新功能位置

启动后，新功能在：

### 工具栏
- 📘 封面配置
- 📐 模板上传

### 菜单栏
- 工具 → 封面和声明页配置
- 快捷键：Ctrl+Shift+C

### 首页
- 🚀 快速开始
- 📤 上传模板
- 📘 封面配置

---

## ❓ 故障排除

### 问题 1: ModuleNotFoundError

**错误：**
```
ModuleNotFoundError: No module named 'PyQt5'
```

**解决：**
```bash
pip install -r requirements.txt
```

### 问题 2: 权限不足

**错误（Linux/Mac）：**
```
Permission denied
```

**解决：**
```bash
chmod +x 启动.sh
./启动.sh
```

### 问题 3: Python 版本过低

**错误：**
```
Python 3.8+ is required
```

**解决：**
```bash
# 检查版本
python3 --version

# 安装 Python 3.8+
# Windows: https://www.python.org/downloads/
# Mac: brew install python@3.8
# Linux: sudo apt install python3.8
```

### 问题 4: 找不到新功能

**原因：** 运行了旧版 main.py

**解决：**
```bash
# 运行新版本
python3 run.py
# 或
python3 main_refactored.py
```

---

## 📞 快速参考

### 启动命令速查

```bash
# Windows
启动.bat

# Linux/Mac
./启动.sh

# 通用
python3 run.py
python3 launcher.py
python3 main_refactored.py
```

### 安装命令速查

```bash
# 安装依赖
pip install -r requirements.txt

# 安装 PyQt5
pip install PyQt5

# 安装 python-docx
pip install python-docx

# 安装 pywinstyles (Windows)
pip install pywinstyles
```

---

## ✅ 检查清单

启动后确认：
- [ ] 窗口标题显示 "论文排版优化器 v2.0"
- [ ] 界面显示正常
- [ ] 工具栏有封面配置按钮
- [ ] 菜单有工具选项
- [ ] 点击按钮有响应

---

## 📚 相关文档

- **快速入门.md** - 使用教程
- **README.md** - 项目说明
- **用户使用手册.md** - 详细指南
- **常见问题解答.md** - FAQ

---

## 🎊 总结

**最简单的方式：**
- Windows: 双击 `启动.bat`
- Linux/Mac: 运行 `./启动.sh`
- 通用：运行 `python3 run.py`

**所有启动方式都指向 `main_refactored.py`（v2.0）**

---

**开发小助手 · 严谨专业版**  
**© 2024 All Rights Reserved**
