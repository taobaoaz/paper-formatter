# 🔧 主程序功能集成说明

**问题：** 运行 main.py 后没有看到新增的封面配置功能

**原因：** 新功能已添加到 main_refactored.py，但 launcher.py 启动的是 main.py

---

## ✅ 解决方案

### 方案 1：运行 main_refactored.py（推荐）

```bash
cd /home/admin/openclaw/workspace/paper-formatter
python3 main_refactored.py
```

**说明：** main_refactored.py 是完整的重构版，包含所有新功能

---

### 方案 2：修改 launcher.py

编辑 `launcher.py`：

```python
# 原来：
from main import main

# 改为：
from main_refactored import main
```

然后运行：
```bash
python3 launcher.py
```

---

### 方案 3：使用快捷脚本

创建 `run_new.py`：

```python
#!/usr/bin/env python3
import sys
import os

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from main_refactored import main

if __name__ == '__main__':
    main()
```

运行：
```bash
python3 run_new.py
```

---

## 📋 新功能位置

### 在 main_refactored.py 中

1. **工具栏按钮**
   - 📘 封面配置
   - 📐 模板上传

2. **菜单**
   - 工具 → 封面和声明页配置
   - 快捷键：Ctrl+Shift+C

3. **首页快捷按钮**
   - 🚀 快速开始
   - 📤 上传模板
   - 📘 封面配置

---

## 🎯 使用新功能

### 方式 1：封面配置器

1. 运行 `python3 main_refactored.py`
2. 点击工具栏 "📘 封面配置"
3. 配置封面信息
   - 手动填写
   - 或上传模板选择页面
4. 保存配置

### 方式 2：模板上传

1. 点击 "📂 上传模板并选择页面"
2. 选择 Word 模板 (.docx)
3. 选择封面页（第 1 页）
4. 选择声明页（第 2 页）
5. 确认保存

---

## 📊 功能对比

| 功能 | main.py | main_refactored.py |
|------|---------|-------------------|
| **封面配置** | ❌ | ✅ |
| **模板上传** | ❌ | ✅ |
| **现代化 UI** | ❌ | ✅ |
| **页面选择** | ❌ | ✅ |
| **稳定性** | ✅ 稳定 | ✅ 测试中 |

---

## 🐛 如果 main.py 也需要新功能

需要将以下功能集成到 main.py：

### 1. 导入模块

在 main.py 开头添加：
```python
from cover_declaration_config import CoverDeclarationConfigDialog, CoverDeclarationManager
from template_page_selector import TemplatePageSelectorDialog, TemplatePageManager
```

### 2. 添加菜单项

在 `create_menu_bar` 方法中添加：
```python
# 工具菜单
tools_menu = menubar.addMenu('工具 (&T)')

cover_config_action = QAction('📘 封面和声明页配置', self)
cover_config_action.setShortcut('Ctrl+Shift+C')
cover_config_action.triggered.connect(self.open_cover_declaration_config)
tools_menu.addAction(cover_config_action)
```

### 3. 添加处理方法

在 MainWindow 类中添加：
```python
def open_cover_declaration_config(self):
    """打开封面和声明页配置对话框"""
    try:
        from cover_declaration_config import CoverDeclarationConfigDialog
        dialog = CoverDeclarationConfigDialog(parent=self)
        if dialog.exec_() == QDialog.Accepted:
            config = dialog.get_config()
            school = config.get('cover', {}).get('school_name', '未设置')
            self.statusBar().showMessage(f'封面配置已更新：{school}')
            QMessageBox.information(self, '成功', '封面配置已保存！')
    except Exception as e:
        QMessageBox.critical(self, '错误', f'打开封面配置失败：{str(e)}')
```

---

## 🚀 推荐做法

**使用 main_refactored.py 作为主程序**

原因：
- ✅ 包含所有新功能
- ✅ 代码结构更清晰
- ✅ UI 更现代化
- ✅ 易于维护

**main.py 作为备用**

- ✅ 稳定版本
- ✅ 兼容旧系统
- ✅ 功能完整

---

## 📞 快速启动命令

```bash
# 推荐：使用新版本
cd /home/admin/openclaw/workspace/paper-formatter
python3 main_refactored.py

# 或者：使用 launcher（需修改）
python3 launcher.py

# 或者：创建快捷脚本
echo '#!/usr/bin/env python3
from main_refactored import main
main()' > run.py
python3 run.py
```

---

## ✅ 检查清单

运行后检查：
- [ ] 窗口标题显示 "论文排版优化器 v2.0"
- [ ] 工具栏有 "📘 封面配置" 按钮
- [ ] 菜单有 "工具 → 封面和声明页配置"
- [ ] 首页有 3 个快捷按钮
- [ ] 点击封面配置能打开对话框

---

**推荐使用 main_refactored.py！** 🚀

---

**开发小助手 · 严谨专业版**  
**© 2024 All Rights Reserved**
