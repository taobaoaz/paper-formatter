name: Build Installer

on:
  push:
    tags:
      - 'v*'

jobs:
  build-installer:
    runs-on: windows-latest
    
    # 设置默认 shell 为 bash，避免编码问题
    defaults:
      run:
        shell: bash
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller
        pip install -r ./启动文件/requirements.txt
    
    - name: Build EXE
      run: |
        pyinstaller --onefile --windowed \
          --name="论文排版优化器" \
          --icon=./installer/app_icon.ico \
          ./核心模块/main.py
    
    - name: Upload EXE
      uses: actions/upload-artifact@v4
      with:
        name: paper-formatter-exe
        path: dist/论文排版优化器.exe
        retention-days: 30
    
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: dist/论文排版优化器.exe
        generate_release_notes: false
        draft: true
        body: |
          ## 📦 论文排版优化器 安装版

          ### 下载地址
          - 点击上方的 `论文排版优化器-EXE.exe` 下载

          ### 安装说明
          1. 下载 `.exe` 文件
          2. 双击运行即可使用

          ### 更新内容
          请查看项目中的 `RELEASE_NOTES_v*.md` 文件获取详细信息。

          ---

          **GitHub Actions Build** | **Draft Release**
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
